# ⚙️ Инструкция по запуску проекта

Этот документ содержит пошаговое руководство по настройке локального окружения и запуску системы.

---

## 🛠 1. Подготовка окружения

Перед запуском убедитесь, что выполнены следующие условия:
* Установлен и запущен **Docker Desktop**.
* **PowerShell** открыт от имени администратора.
* Вы находитесь в корневой директории проекта.
* Необходимо перейти в директорию с файлаби в data
```powershell
cd .../data
```

---

## 🐳 2.1 Развертывание контейнеров

Для инициализации инфраструктуры и базы данных выполните команду ниже. Она остановит старые контейнеры (если они были) и поднимет новые в фоновом режиме. 

> **Важно:** выполняется один раз при первом запуске или при необходимости полного сброса данных.

```powershell
docker-compose down(остонавливает); docker-compose up -d(запускает)
```

## 🔍 2.2 Решение проблем с загрузкой (Docker Mirror)

Если образы не скачиваются, перейдите в Settings -> Docker Engine и замените конфигурацию на следующую:

```powershell
{
  "builder": {
    "gc": {
      "defaultKeepStorage": "20GB",
      "enabled": true
    }
  },
  "experimental": false,
  "registry-mirrors": [
    "https://mirror.gcr.io",
    "https://huecker.io",
    "https://dockerhub.timeweb.cloud",
    "https://registry.docker-cn.com"
  ]
}

```

---

## 🚀 3. Запуск приложения

Запуск основного скрипта. После того как контейнеры успешно запустились, выполните запуск самого приложения:

```powershell
.\start.ps1
```

> **Важно:** Если возникла ошибка выполнения скрипта: В Windows по умолчанию может быть запрещен запуск сторонних скриптов. В этом случае используйте команду: 
```powershell
powershell -ExecutionPolicy Bypass -File .\start.ps1
```

---

## 📊 4. Визуализация данных

После запуска приложения вы можете просмотреть сгенерированные диаграммы и отчеты через веб-интерфейс.

* **Ссылка для доступа:** [http://localhost:8501/](http://localhost:8501/)
* **Инструмент:** Streamlit (или Dashboard проекта)

> **Важно:** Веб-интерфейс станет доступен только после того, как соответствующий контейнер полностью инициализируется. Если страница не открывается сразу, подождите 10–15 секунд.
